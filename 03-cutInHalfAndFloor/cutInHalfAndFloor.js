function cutInHalfAndFloor(num) {
  // Insert code here;
  var halfNum = (num / 2);
  var number = Math.floor(halfNum);
  return number;
}

// Do not edit this line;
module.exports = cutInHalfAndFloor;
